[build]
  command = "npm run build"
  publish = "build"

[build.environment]
  NODE_VERSION = "16" # veya kullandığınız sürüm

# API proxy ayarları için (eğer backend'inizi başka bir serviste barındırıyorsanız)
# [[redirects]]
#   from = "/api/*"
#   to = "https://your-backend-url.com/api/:splat"
#   status = 200 